# Minimale .htaccess voor Aggregaatkopen.com
# Dit bestand voorkomt redirect loops

# Basic security
Header always set X-Content-Type-Options nosniff
Header always set X-Frame-Options DENY

# Ensure index.html is served for directory access
DirectoryIndex index.html

# Allow access to essential files
<Files "robots.txt">
    Allow from all
</Files>

<Files "sitemap.xml">
    Allow from all
</Files>

<Files "*.css">
    Allow from all
</Files>

<Files "*.js">
    Allow from all
</Files>

# No redirects to prevent loops
# We'll add redirects later after confirming basic setup works
# VOLLEDIG REDIRECT-VRIJE .htaccess voor Aggregaatkopen.com
# Geen enkele redirect om loops te voorkomen

# DirectoryIndex
DirectoryIndex index.html

# Basic headers (NO redirects of any kind)
Header always set X-Content-Type-Options nosniff
Header always set X-Frame-Options DENY

# GZIP Compression
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript
</IfModule>

# Allow all essential files
<Files "*">
    Allow from all
</Files>

# ABSOLUTELY NO REWRITES OR REDIRECTS